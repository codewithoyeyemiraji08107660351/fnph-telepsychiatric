spring:
  datasource:
    url: jdbc:mysql://localhost:3306/telepsychiatric_test?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  mail:
    host: localhost
    port: 1025
  data:
    redis:
      host: localhost
      port: 6379

application:
  security:
    jwt:
      secret: test-only-secret-not-for-any-real-deployment-0123456789abcdef
  encryption:
    key: dGVzdC1vbmx5LWtleS0zMi1ieXRlcy1sb25nLXh4
  storage:
    root: ./target/test-storage